// Generated by CoffeeScript 1.4.0
(function() {
  var attach;

  attach = function(element, doc, disabled) {
    if (disabled == null) {
      disabled = false;
    }
    elem.disabled = disabled;
    doc.attach_textarea(elem);
    return console.log('vivace textarea shared');
  };

  sharejs.open('default', 'text', function(error, doc) {
    if (error) {
      console.log(error);
    }
    if (!error) {
      return attach(document.getElementById('code'), doc);
    }
  });

}).call(this);
