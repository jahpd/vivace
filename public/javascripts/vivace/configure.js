// Generated by CoffeeScript 1.4.0
(function() {
  var load;

  load = function(scripts) {
    return $(document).ready(function() {
      return $.each(scripts, function(script, callback) {
        return Vivace.onload(script, callback);
      });
    });
  };

  load({
    'vivace_lang': function() {},
    'init': function() {
      return Vivace.init(function(voicename) {
        return console.log(voicename + ' initialized');
      });
    },
    'mixer': function() {},
    'loadMedias': function() {
      return Vivace.loadMedias(function(name, voice, loader) {
        console.log(name + ' loading ' + voice.sig + '...');
        if (voice.sigType === 'audio') {
          loader(name, voice.sig, Vivace.mixer.createChannel);
        }
        if (voice.sigType === 'video') {
          return loader(name, voice.sig);
        }
      });
    },
    'menu': function() {
      return Vivace.menu.create('#banner', Vivace.menu.banner, function(menuitem) {
        return console.log(menuitem + ' added');
      });
    },
    'play': function() {},
    'tick': function() {},
    'exec': function() {},
    'keyboard': function() {
      return Vivace.keyboard.create(function(keycontrols) {
        return $.each(keycontrols, function(k, keys) {
          return console.log('add listener to button ' + k);
        });
      });
    },
    'run': function() {
      return $('div.dg:last').ready(function() {
        return Vivace.run();
      });
    }
  });

}).call(this);
