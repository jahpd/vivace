// Generated by CoffeeScript 1.4.0
(function() {
  var playVideo, playVoice;

  playVoice = function(voicename, time, offset, duration) {
    var source, voice;
    voice = Vivace.voices[voicename];
    try {
      source = voice.audionodes.src;
    } catch (e) {
      console.log(e);
    }
    if (time && offset && duration && voice.isAvailable) {
      return source.noteGrainOn(time, offset, duration);
    } else {
      return source.noteGrainOn(context.currentTime, 0, source.buffer.duration);
    }
  };

  playVideo = function(voicename, offset) {
    return window.Vivace.voices[voicename].sigPop.play(offset);
  };

  window.Vivace.playVoice = playVoice;

  window.Vivace.playVideo = playVideo;

}).call(this);
